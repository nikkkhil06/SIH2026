# GreenFleetQ React UI
Three-page MVP frontend:
- `/` Dashboard
- `/optimize` Create Optimization Scenario
- `/results` Results

Currently uses mock data. The Optimize button navigates to Results.
Later replace that part with `fetch()` to Ninad's backend API.

Run:
npm install
npm run dev
